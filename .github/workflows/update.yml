name: Update My Stats!
on:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches: ["1-try-with-github-actions"]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: 
          ref: '1-try-with-github-actions'